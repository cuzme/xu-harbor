{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["es/**", "lib/**", "dist/**"]
    },
    "lint": {},
    "lint:fix": {},
    "format": {},
    "format:check": {}
  }
}
